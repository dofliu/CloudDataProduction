# ── P0 核心相依 ──────────────────────────────────────────────
# 引擎與服務同一進程(asyncio + FastAPI),見 docs/01。
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
PyYAML>=6.0
pydantic>=2.6
python-dotenv>=1.0

# Modbus TCP 轉接層 —— 版本鎖定,勿升級(見 CLAUDE.md 技術棧)
pymodbus==3.6.9

# Historian:TimescaleDB(PostgreSQL)非同步驅動
asyncpg>=0.29

# P1:OPC-UA server / MQTT(內嵌純 Python broker,免 Docker)
asyncua>=1.1
amqtt>=0.11

# P2:MCP server(老師本機 Claude Desktop;薄 REST 轉接)
fastmcp>=3.0
httpx>=0.27

# 自然語言建廠目前走規則式(免 key);要改用 LLM 再裝對應 SDK,例:
# google-generativeai>=0.5   # Gemini(設 GEMINI_API_KEY)
