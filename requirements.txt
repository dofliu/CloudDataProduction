# ── P0 核心相依 ──────────────────────────────────────────────
# 引擎與服務同一進程(asyncio + FastAPI),見 docs/01。
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
PyYAML>=6.0
pydantic>=2.6
python-dotenv>=1.0

# Modbus TCP 轉接層 —— 版本鎖定,勿升級(見 CLAUDE.md 技術棧)
pymodbus==3.6.9

# Historian:TimescaleDB(PostgreSQL)非同步驅動
asyncpg>=0.29

# P1:OPC-UA server / MQTT(內嵌純 Python broker,免 Docker)
asyncua>=1.1
amqtt>=0.11

# ── 後續 phase 會用到,先註解保留 ────────────────────────
# fastmcp>=0.2            # MCP server(老師本機)
